<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbs.example.lolHi.dao.ArticleDao">
	<select id="getArticles" resultType="Article">
		SELECT A.*
		FROM article AS A
	</select>

	<select id="getArticleByNum" resultType="Article">
		SELECT A.*
		FROM article AS A
		WHERE num = #{num}
	</select>

	<delete id="doDeleteArticleByNum">
		DELETE FROM article
		WHERE num = #{num}
	</delete>

	<select id="showModifyArticleByNum" resultType="Article">
		SELECT A.*
		FROM article AS A
		WHERE num = #{num}
	</select>

	<update id="doModifyArticleByNum">
		UPDATE article

		<set>
			updateDate = NOW(),
			<if test="title != null">
				title = #{title},
			</if>
			<if test="body != null">
				`body` = #{body},
			</if>
		</set>
		WHERE num = #{num}
	</update>

	<insert id="doWriteArticleByNum" useGeneratedKeys="true" keyProperty="num">
		INSERT INTO article
		<set>
			regDate = NOW(),
			updateDate = NOW(),
			title = #{title},
			body = #{body}
		</set>
	</insert>
</mapper>